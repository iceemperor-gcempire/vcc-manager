# v2 업데이트 내역

## v2.6.0

### 신규 기능
- feat: 프로젝트 세계관 (사전 컨텍스트) + 워크보드 소속 (#396 Phase 1.A, #398)
  - `Tag.isWorldviewTag` 추가 + migration 으로 사용자별 \"세계관\" 태그 자동 시드. 신규 사용자는 `GET /api/tags/worldview` 가 lazy 생성.
  - `Project.workboardIds` 추가 — 프로젝트가 작업판 목록을 보유 (단방향). 한 작업판이 여러 프로젝트에 들어갈 수 있음.
  - 세계관 = `UploadedText` + `[프로젝트 태그, 세계관 태그]` 조합 (새 모델 / 컬렉션 없음, 1:N 자연스럽게 지원)
  - 프로젝트 상세 페이지에 \"작업판\" / \"세계관\" / \"대화 히스토리\" 탭 추가. 작업판 카드별 \"실행\" 버튼이 프로젝트 컨텍스트 자동 전달.
  - 작업판 실행 페이지 (`/prompt-generate/:wbId`) 에 `ProjectContextSelector` — 프로젝트 드롭다운 + 세계관 사용 토글.
  - `prompt-generate` 가 `projectId + useWorldview` 받으면 해당 프로젝트의 세계관 텍스트를 system 메시지 `[작업 지침]` / `[배경 / 사전 컨텍스트]` 두 섹션으로 합성.
  - `ConversationJob` 에 `projectId` / `workboardSystemPrompt` / `worldviewContext` 분리 저장. 대화 상세 다이얼로그에 별도 카드 노출.
- feat: 프로젝트 종속 작업판 파이프라인 (#397 Phase 2.A, #399)
  - `Pipeline` 모델 + `/api/projects/:projectId/pipelines` CRUD. 프로젝트 상세에 \"파이프라인\" 탭 추가.
  - 빌더: 단계 리스트 (작업판 picker, 위/아래 reorder, **단계별 자동 주입 토글**, **단계별 사전 입력 dialog** — type-aware 입력기 (`string/number/boolean/select/baseModel/lora`), **단계별 메모**)
  - 실행 (클라이언트 측 순차 오케스트레이션): Stepper + 단계별 상태, 자동 매핑 (텍스트 출력 → 다음 prompt 필드 / 이미지 출력 → image 필드), 사전 입력 + 자동 주입 + 초기 프롬프트 우선순위, 이미지/영상 폴링 (5초/최대 5분)
  - 파이프라인 전체 \"세계관 자동 주입\" 토글 (첫 LLM 단계에 적용)
- feat: 프로젝트 작업 태그 통일 (#397 후속)
  - 프로젝트 컨텍스트로 수행되는 모든 작업이 자동으로 프로젝트 태그를 받도록 통일
  - `ConversationJob` 에 `tags` 필드 추가 + 기존 데이터 backfill migration
  - `/api/projects/:id/conversations` 필터를 `projectId OR tags` union 으로 변경 (기존 + 신규 데이터 모두 포함)
  - 파이프라인 runner — 이미지/영상 단계 실행 시 `inputData.tags` 에 프로젝트 태그 주입 (큐 → GeneratedImage / GeneratedVideo 까지 전파)

### 수정 / UI
- `customField` 기본값 / 설명 / 플레이스홀더 — 작업판 admin 편집기에서 type-aware 입력 (baseModel / lora 는 서버 메타데이터 Autocomplete) — v2.5.0 부터 보존되던 거 그대로
- ProjectDetail 모바일 헤더 / 탭 / 우측 버튼 압축, \"이미지 생성하기\" → \"작업판 보기\" 라벨 변경 (텍스트 작업판 포함)
- ProjectDetail 탭 위치 localStorage 영속화 (프로젝트별 별도 키)
- 빌더 select 필드 placeholder 겹침 fix (`InputLabelProps={{ shrink: true }}`)
- 빌더 cache invalidation 누락 fix (편집 후 재진입 시 stale 데이터 표시되던 문제)
- 파이프라인 list 카드 / 빌더 / 실행 stepper 에 작업판 description + 단계 note 노출

## v2.5.0

### 신규 기능
- feat: 텍스트 컨텐츠 저장소 + 내 이미지 → 내 컨텐츠 통합 (#387, #388)
  - `UploadedText` (직접 작성, 본문 100만자) / `GeneratedText` (대화에서 명시 저장) 두 모델
  - `/api/texts/uploaded` + `/api/texts/generated` CRUD. `GeneratedText` POST 는 `conversationJobId + messageIndex` 로 assistant 메시지 스냅샷 저장 (원본 대화 삭제돼도 텍스트 보존)
  - 사이드바 라벨 "내 이미지" → "내 컨텐츠", 라우트 `/images` → `/content` (구 경로 redirect)
  - 페이지에 텍스트 탭 2종 추가 (직접 작성 / 생성). 모바일 5탭 수용 위해 `Tabs variant="scrollable"`
  - `TextContentPanel` 공용 컴포넌트 — 카드 리스트, 검색, 편집 다이얼로그 (uploaded), 상세 다이얼로그 (generated 도 태그 편집 가능), 복사 / 삭제 / + FAB
  - 대화 히스토리 / 채팅 패널의 assistant 메시지에 북마크 아이콘 → `GeneratedText` 생성
- feat: 텍스트 파일 업로드 (#389, #390)
  - 직접 작성 텍스트 탭에 + FAB 와 분리된 "파일 업로드" 버튼 (향후 직접 작성을 프로젝트 전용으로 제한할 수 있게 진입점 분리)
  - 다중 파일 (.txt, .md), 각 5MB, react-dropzone 기반. 파일명 → title (확장자 제외), 일괄 태그 적용, 파일별 상태 표시
- feat: 텍스트 작업판 멀티턴 대화 모드 (#391, #392)
  - 텍스트 작업판 customField 의 `conversation_mode` boolean 토글 ON 시 단일 샷 프롬프트 생성 UI 대신 채팅 UI 로 동작
  - `WorkboardChatPanel` — fresh new conversation 으로 시작, 상단 collapsible 설정 패널 (model/sysprompt/temperature 등), 첫 메시지 전송 후 lock+collapse
  - 매번 새 대화로 시작. 기존 대화 이어가려면 히스토리의 "계속하기" 사용
  - 텍스트 템플릿 3종 (OpenAI / Gemini / OpenAI Compatible) 에 `conversation_mode` 필드 기본 포함

### 수정
- fix: customField 기본값 / 설명 / 플레이스홀더 admin 편집기 (#391, #392)
  - 기존 form load / save 흐름이 `defaultValue` / `description` / `placeholder` 를 드롭해 admin 이 사전정의값을 넣을 수 없던 문제 — load 와 save 양쪽에서 보존
  - type-aware 기본값 입력기: boolean → Switch, select → 옵션 드롭다운, baseModel / lora → 서버 메타데이터 Autocomplete, string / number → TextField
- fix: max_tokens 제거 — 텍스트 출력 타입 전체 (#391, #392)
  - 두 chat 서비스 (`openAIChatService`, `geminiService`) 모두 이미 모델 기본값을 사용하고 있어 안전 제거. 템플릿 3종 + `routes/jobs.js` 정리.
- fix: 대화 히스토리 카드 액션 버튼을 이미지 카드와 통일 (#391 후속, #393)
  - 텍스트는 사실상 "계속하기" 가 핵심인데 눈동자 IconButton 만 노출되어 상세 다이얼로그 안에 숨어 있던 문제
  - 카드에 "상세" Button + "계속하기" success contained 직접 노출 (이미지 카드 패턴)

## v2.4.1

### 수정
- fix: 모바일 가로 오버플로우 — 모델 관리 / 모델 picker 의 리스트 / 이미지 리스트 뷰에서 긴 모델명·파일명 때문에 행이 viewport 보다 넓어져 body 전체 가로 스크롤이 발생하던 문제 (#383, #384)
  - 실제 root cause 는 `App.js` 의 `<Box component="main">` 이 flex item 인데 `minWidth: 0` 누락. flex 기본 `min-width: auto` 가 content intrinsic width 로 작동해 main 박스 자체가 viewport 를 넘김.
  - main Box 에 `minWidth: 0` + `overflowX: hidden` 추가. 행 outer Box 도 `width: 100%`, `minWidth: 0`, `overflow: hidden` 보강 (defense in depth).
  - baseModel 텍스트는 `flexShrink: 1` + `maxWidth: xs 80 / sm 160` + ellipsis. 액션 버튼은 `flexShrink: 0, minWidth: 'auto'`.
- fix: 모바일 모델 관리 페이지 미세 정렬 (#383 후속, #385)
  - CivitaiAdminHeader 의 NSFW 토글 두 개를 `Stack row + flexWrap` 에서 `direction={{ xs: 'column' }}` 로 변경. FormControlLabel 의 기본 음수 marginLeft (-11px) 도 0 으로 명시.
  - 캐시 정보 카드: `Stack(flexWrap)` → `Grid container`. 모바일 2×2, 데스크탑 1×4. 이전엔 4번째 컬럼 (`마지막 메타 동기화`) 이 첫 번째 컬럼 (`서버`) 바로 아래에 wrap 되어 한 컬럼처럼 보이는 문제.

## v2.4.0

### 신규 기능
- feat: LLM 텍스트 대화 히스토리 (Phase 1, #373, #374)
  - `ConversationJob` 모델 추가 — `messages: []` 구조로 단문 Q&A 와 향후 멀티턴 동시 호환
  - `/api/conversations` 라우트 (`/my`, `/:id`, `/all` admin, 삭제)
  - `prompt-generate` 호출 직전 `processing` → 실패 시 `failed` + error 메시지 → 성공 시 `assistant` append + `completed`
  - 작업 히스토리 페이지에 **이미지 / 텍스트** 탭 추가, 텍스트 탭은 카드 리스트 + 상세 다이얼로그 (role 별 아이콘)
- feat: LLM 대화 멀티턴 이어가기 (Phase 2, #375, #376)
  - 작업판 페이지가 `?conversationId=` 쿼리 감지 시 `PromptGeneratorPanel` 대신 `ConversationChatPanel` 로 전환 — 이전 messages 누적 표시 + 다음 메시지 입력
  - `prompt-generate` 가 `conversationId` 옵션 받으면 기존 대화에 user/assistant append 후 LLM 에 전체 messages 시퀀스 전달
  - 히스토리 상세 다이얼로그에 "이 대화 이어가기" 버튼, 카드에 `N턴` 칩
- feat: LLM 텍스트 대화 비용 추정 (Phase 3, #377, #378)
  - `pricing.js` 에 텍스트 모델 가격표 추가 (USD per 1M tokens)
    - OpenAI: `gpt-5.5`, `gpt-5.5-pro`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-5.4-pro`
    - Gemini: `gemini-3.1-flash-lite`, `gemini-3.1-flash-lite-preview`, `gemini-3.1-pro-preview` (200K context tier)
  - `prompt-generate` 가 chat 호출 성공 후 비용 산출해 `ConversationJob.costEstimate` 누적 저장 (멀티턴 시 턴별 합산)
  - 히스토리 상세 + 채팅 헤더에 누적 비용 노출

### 수정
- fix: prompt-generate 가 작업판 customField 의 `base_model` 값을 인식하지 못해 항상 `gpt-4` fallback 으로 저장되던 버그 해결 (#377)
  - model / systemPrompt / temperature / maxTokens 를 `getFieldValueByRole` 기반 lookup 으로 일원화
  - legacy `baseInputFields` fallback chain 제거 — F2 마이그레이션 완료 후 DB 잔존 없음
  - 멀티턴은 conversation 에 저장된 model 유지 (작업판 변경에도 일관성)
- fix: PromptGeneratorPanel 하단 stray `)}` 잔여물 제거 — F2 systemPrompt UI 제거 리팩토링 잔재가 텍스트로 렌더되던 문제

### 보안
- security: `TEST_AUTO_ADMIN_DOMAIN` 백도어 완전 제거 (#379, #380)
  - dev 환경이 alpha 도메인으로 외부 노출되어 있어 `@example.com` 자동 admin 승격이 공격 표면이 됨
  - docker-compose.yml 기본값 + `User.updateAdminStatus` 코드 경로 모두 제거
  - E2E 테스트는 후속 작업에서 시크릿 헤더 / DB seed 등 안전한 admin 부여 방식으로 재설계 필요

## v2.3.2

### 수정
- fix: admin 작업판 관리 페이지의 검색 / serverType / outputFormat 필터 localStorage 영속 (#370, #371)
  - 사용자용 \`Workboards\` 는 이미 영속됐지만 admin \`WorkboardManagement\` 는 useState 만 사용해 페이지 재진입 시 필터 초기화되던 문제. 일관성 확보.
  - 키 prefix \`vcc.adminWorkboards.\` 로 사용자용과 분리.

## v2.3.1

### 신규 기능
- feat: Gemini 이미지 생성 비용 추정 (#367, #368, #364 Phase 2)
  - 대상: \`gemini-2.5-flash-image(-preview)\` / \`gemini-3-pro-image-preview\` / \`gemini-3.1-flash-image-preview\`
  - \`response.usageMetadata\` 의 \`promptTokenCount\` / \`candidatesTokenCount\` 로 \`pricing.js\` 의 \`GEMINI_IMAGE_PRICING\` 적용
  - JobHistoryPanel 의 작업 상세에 OpenAI 와 동일한 \"추정 비용 + 토큰 breakdown\" 표시
  - 제외: \`imagen-*\` (per-image 정액 — 토큰 기반 추정 불가, 별도 처리 필요)
  - Gemini 응답은 입력 텍스트/이미지 구분 없어 \`inputImageTokens\` 칸에 \`promptTokenCount\` 일괄 보관

## v2.3.0

### 신규 기능
- feat: OpenAI 이미지 생성 비용 추정 + 히스토리 저장 (#364, #365)
  - 대상: \`gpt-image-1\` / \`gpt-image-1-mini\` / \`gpt-image-1.5\` / \`gpt-image-2\` (DALL-E 제외)
  - OpenAI 응답의 \`usage\` (input text / input image / output token) 를 캐시하고 자체 가격표 (\`src/utils/pricing.js\`, \`PRICING_VERSION=2026-05\`) 로 비용 산출
  - \`ImageGenerationJob\` 스키마에 \`usage\`, \`costEstimate\` 필드 추가
  - JobHistoryPanel 의 작업 상세에 \"추정 비용 (\$amount, pricingVersion)\" + 토큰 breakdown 표시
  - admin billing API 키 불필요 — provider 가 응답에 토큰 사용량 포함하는 점만 활용
  - 가격표 갱신 시 \`PRICING_VERSION\` 도 함께 올려야 과거 저장된 추정치 시점 추적 가능

### 향후 (별도 이슈)
- Phase 2: Gemini 이미지 생성 (Imagen / nano-banana 등) usage 매핑

## v2.2.3

### 신규 기능
- feat: 화이트리스트 picker — 이미 선택된 항목 시각화 + 토글 + 카운트 (#277, #358)
  - admin 작업판의 modelWhitelist / loraWhitelist picker (multi-add 모드) UX 개선
  - 모달 상단에 \"N개 선택됨\" chip 표시. 카드/리스트 항목의 selected 상태는 highlight + 버튼 라벨이 \"제거\" 로 토글. 같은 카드 다시 클릭 시 화이트리스트에서 제거.
- feat: Civitai API 키 스키마를 \`settings.external.civitaiApiKey\` 로 이전 (#293 Phase B, #356)
  - \"LoRA namespace 라 베이스 모델은 별도 키?\" 오해 유발하던 위치 정정. legacy 위치도 fallback 으로 유지, 자동 마이그레이션 멱등.
- feat: Playwright E2E 테스트 도입 (#359, #360)
  - root level \`e2e/\` + 5개 critical journey smoke (랜딩 / 대시보드 / 작업판 / 내 이미지 / signup)
  - \`npm run test:e2e\` / \`test:e2e:ui\` / \`test:e2e:headed\`
  - CLAUDE.md 정책: UI 변경 시 관련 e2e spec 동시 업데이트
  - dev/e2e 전용 env: \`SIGNUP_RATE_LIMIT_MAX\` (rate limit 완화), \`TEST_AUTO_ADMIN_DOMAIN\` (test 사용자 admin 자동 승격). 프로덕션 미설정 — 영향 없음

### 문서
- docs: CLAUDE.md 의 이슈 / git 정책 중복 섹션 통합 (#361, #362)
  - 하단 \"GitHub Issue 처리\" 의 절차 step 제거 — 상단 \"작업 Workflow\" 와 중복. issue 참조 syntax + 보강 코멘트 정책만 잔존.
  - \"작업 Workflow\" 의 nested sub-step 정리.

## v2.2.2

### 신규 기능
- feat: OpenAI / Gemini 모델의 outputFormat 추론 + workboard 기반 picker 필터 (#354, #355)
  - OpenAI: id 패턴 (gpt-image / dall-e / sora → image, gpt / chatgpt / o1 / o3 / o4 → text). 그 외 (whisper / tts / embedding 등) 미지원으로 picker 미노출.
  - Gemini: \`supportedGenerationMethods\` 우선 (predict → image, generateContent → name 패턴 추가 검사), name/displayName/description 통합 검사 (imagen / image-generation / image-preview / image-edit / nano-banana / -image-/-image\$ 모두 image 분류)
  - \`provider.outputFormats: [String]\` 신규 schema 필드
  - workboard 의 outputFormat 으로 picker 자동 필터. 사용자 / admin 화이트리스트 양쪽 적용.
- feat: admin 작업판 화이트리스트 picker 활성화 (모든 server type) (#354)
  - 기존 ComfyUI 만 가능했던 \"선택\" 버튼이 OpenAI / Gemini 등 모든 서버에서 활성. workboard outputFormat 으로 자동 필터.
- feat: Civitai API 키 스키마를 \`settings.external.civitaiApiKey\` 로 이전 (#293 Phase B, #356)
  - 기존 \`settings.lora.civitaiApiKey\` (LoRA namespace 라 \"베이스 모델은 별도 키?\" 오해 유발) → \`settings.external\` (외부 서비스 통합 namespace)
  - 부팅 시 자동 마이그레이션 (멱등). legacy 위치도 fallback 으로 유지.

### 수정
- fix: API GET 응답에 \`Cache-Control: no-store\` 명시 (#354)
  - admin 동기화 후 picker 가 같은 URL 캐시 재사용해 이전 모델 목록을 표시하던 문제. \`/api/*\` 미들웨어에서 모든 GET 응답에 cache 비활성 헤더 추가.
- fix: picker 의 vcc-file-hash 미설치 안내 Alert 제거 (#354)
  - SaaS provider (OpenAI / Gemini) 작업판에서도 잘못 노출되던 안내. admin 페이지에는 serverType 게이트와 함께 잔존.
- fix: picker Dialog 의 height 를 \`85vh\` 고정 (#354)
  - 검색 글자 입력 시 fetch loading / pagination 변화로 다이얼로그 전체가 덜컹거리던 문제.

### 문서
- docs: INSTALLATION.md 에 \"초기 모델 / LoRA 동기화\" 섹션 추가 (#352, #353)
  - 신규 설치 admin 의 첫 작업 흐름 (서버 등록 → vcc-file-hash 노드 설치 → Civitai API 키 → 동기화 → 캐시 삭제) 단계별 안내. v2.2 의 공용 헤더 / 3종 view mode / NSFW 토글도 함께 설명.

## v2.2.1

### 신규 기능
- feat: ComfyUI \`folder_paths.cached_filename_list_\` 의 stale 자동 해결 (#349, #350)
  - \`vcc-file-hash\` 커스텀 노드 v3.0 → v3.1: 신규 \`POST /api/vcc/file-hash/refresh/{folder_type}\` endpoint — folder 캐시 무효화 + 즉시 재스캔.
  - vcc-manager 동기화 (모델 / LoRA) 시작 시 자동 호출 — best-effort, 구 노드(v3.0 이하) 환경에서는 404 silently fallback.
  - 효과: 파일 시스템에서 모델 삭제 후 ComfyUI 재시작 없이도 vcc-manager \"동기화\" 1회로 삭제된 모델이 목록에서 자동 제거.

### 사용자 작업
- ComfyUI 의 \`custom_nodes/comfyui-vcc-file-hash/\` 를 v3.1 로 업데이트 + ComfyUI 재시작 1회. 이후엔 자동.

## v2.2.0

### 모델 관리 UI 통합 + 3종 view mode
\"베이스 모델 / LoRA 는 2종의 모델\" 이라는 관점으로 UI 통합 + view mode 확장.

- feat: 모델 관리 admin 페이지 (\"베이스 모델\" / \"LoRA\" 탭) 본문 컴포넌트 통합 (#344, #345)
  - 신규 \`MetadataManagementBody\` — kind 별 adapter (fetch / sync / clearCache / normalize / labels) 로 분기. 두 탭이 동일한 검색 / view mode 토글 / 동기화·캐시 삭제·리셋 / cache info 패널 / 결과 영역 / 디테일·캐시 다이얼로그 사용.
  - 페이지별 차이 (서버 타입 필터 등) 는 부모 \`MetadataManagementPage\` 가 props 로 주입. \`ModelManagementPage\` / \`LoraManagementPage\` 는 얇은 wrapper 로 축소 (~1000 줄 감소).
  - 검색·필터 / view mode·액션 버튼을 2행으로 분리하여 폭 변경 시 레이아웃 깨짐 / 거대 공백 방지.
- feat: 3종 view mode 신설 (#344)
  - \`grid\` — 기존 카드 그리드
  - \`image-list\` — 신규. 좌측 썸네일 (90px), 우측 모델명 + 버전 / kind·baseModel chip / 트리거 워드 또는 파일명 / 액션 (디테일·civitai 링크·primary 버튼)
  - \`list\` — 텍스트 한 줄 (이름·파일명·baseModel·상세 버튼)
  - 모드 선택은 kind 별 localStorage 영속화
- feat: 작업판 picker 에도 3종 view mode 적용 (#346, #347)
  - admin 페이지와 동일한 grid / image-list / list 토글 노출. image-list 는 admin 과 동일 컴포넌트 재사용.
  - picker 의 \"동기화\" 버튼은 제거 — 동기화는 admin 페이지에서만 수행. (불필요한 노출 정리)
- feat: NSFW 모델 숨김 토글 통일 (#346)
  - picker 의 NSFW item 필터를 \`nsfwLoraFilter\` → \`nsfwModelFilter\` user preference 로 승격. 베이스 모델 / LoRA picker 양쪽에서 동일한 \"NSFW 모델 숨기기\" 토글 노출.
  - legacy \`nsfwLoraFilter\` 는 fallback 으로 유지 (기존 설정 보존).

## v2.1.4

### 신규 기능
- feat: 베이스 모델 / LoRA 카드 UI 개선 (#333, #334)
  - 모델명 아래에 \"버전: <versionName>\" 캡션 별도 표시 (동일 모델 여러 버전 구분 용이)
  - 파일 경로는 카드에서 제거 → 신규 \"상세 정보 (ℹ️)\" 다이얼로그에서만 표시
  - 디테일 다이얼로그: 모델명+버전 / kind·baseModel·capabilities chip / 미디어 그리드 / 설명 / 트리거 워드 전체 / 파일 경로(복사 버튼) / hash / civitai 링크
- feat: 모델 관리 admin 페이지에 공용 헤더 추가 (#337, #338)
  - 타이틀과 탭 사이에 서버 선택기 + NSFW 이미지 / NSFW 모델 토글 + Civitai API key 입력 통합
  - 베이스 모델 ↔ LoRA 탭이 동일 서버 선택을 공유 (localStorage 영속). 탭별 호환 서버 fallback.
  - 동기화 / 캐시 작업 버튼은 탭별로 유지
- feat: 모델 / LoRA \"캐시 완전 삭제\" 버튼 추가 (#341, #342)
  - admin only. \`DELETE /api/servers/:id/(models|loras)/cache\` 호출
  - hash 와 civitai 메타데이터 모두 비움 → 다음 동기화는 hash 부터 재계산
  - 일반 \"동기화\" 는 기존 hash 를 재사용하므로 빠름. 캐시 wipe 가 필요한 시점만 명시적으로 사용

### 수정
- fix: civitai NSFW 모델 / LoRA 바로가기 링크를 \`civitai.red\` 도메인으로 (#331, #332)
  - civitai 정책 변경 대응. nsfw 플래그에 따라 \`civitai.red\` / \`civitai.com\` 호스트 분기
- fix: 동기화 버튼이 항상 강제 재동기화로 동작 (#335, #336)
  - 신규 civitai 메타 필드 (versionName, civitai.red URL) 가 기존 캐시에도 즉시 반영되도록 \`forceRefresh: true\` 디폴트화
  - hash 는 \`existing.hash\` 가 있으면 재사용되어 비용 증가 없음 (#341 코멘트 참고)
  - LoRA 관리 페이지의 별도 \"강제\" 버튼은 일반 동기화와 동일해지므로 제거
- fix: NSFW 모델 숨기기 토글을 공용 헤더로 (#339, #340)
  - 기존 \"NSFW LoRA 숨기기\" 가 LoRA 관리 페이지에만 있던 문제 — 베이스 모델에도 동일 필터 부재
  - backend \`settings.lora.nsfwLoraFilter\` → \`nsfwModelFilter\` 로 의미 승격 (legacy 필드 fallback 유지)
  - 베이스 모델 / LoRA 카드 그리드 양쪽에서 \`civitai.nsfw=true\` 항목 동시 필터링

## v2.1.3

### 수정
- fix: 작업판 모델 타입 필터 시 civitai 미등록 모델 숨김 (#320, #322)
  - 작업판에 `allowedModelTypes` (허용 베이스 모델 타입) 가 설정된 경우, civitai 메타데이터가 없는(미등록) 모델은 베이스 모델 셀렉터에서 노출되지 않음
  - 필터가 없는 작업판은 기존대로 미등록 모델도 노출
  - `#199 Phase E` 의 picker 컴포넌트 통합 시 `MetadataFieldInput` 이 `allowedModelTypes` 를 picker 로 전달하지 않던 회귀도 함께 수정 — 백엔드 필터가 호출되지 않던 상태였음
- fix: 모델 선택 모달의 비디오 썸네일이 hover 전에 백색으로 보이던 문제 (#321, #323)
  - civitai CDN 의 `/anim=false,width=N/` transform 으로 첫 프레임 정지 이미지를 받아 `<video>` 의 `poster` 로 지정
  - 비-civitai 비디오 URL 은 기존 동작 유지 (poster 미설정)
- fix: 모델 / LoRA picker 모달 처음 열 때 그리드가 한 번 깜박이던 문제 (#324, #325)
  - `MetadataPickerModal` 의 debounce useEffect 가 deps 에 `open` 을 포함하고 있어, 모달 open 시 즉시 fetch + 300ms 뒤 debounced fetch 가 동시에 발생
  - debounce useEffect 의 deps 에서 `open` 제거, open 시 최초 fetch 는 별도 useEffect 가 단독 담당

### 기타
- feat: vcc-manager 프로젝트 한정 Claude Code skill 4종 추가 (#318, #319)

## v2.1.2

### 워크플로우 변수 정리
- chore: \"사용 가능한 워크플로우 변수\" 중복 표시 제거 (#312, #316)
  - customField formatString 과 매칭되는 built-in 은 \"기본 변수\" 섹션에서 hide (예: \`{{##base_model##}}\` 가 \"사용자 정의 변수\" 와 \"기본 변수\" 양쪽에 노출되던 문제)
- chore: built-in 추가 정리 (#315, #316)
  - 제거: \`{{##reference_method##}}\` (\`{{##reference_image_method##}}\` 가 대체)
  - 제거: \`{{##upscale_method##}}\` / \`{{##base_style##}}\` — admin 이 customField 로 직접 정의
  - 이동: \`{{##user_id##}}\` 를 \"기본 변수\" 카테고리로 (시스템 제공 값)
  - \"추가 기능\" 카테고리 자체 제거 (basic 만 남음)

### 정책
- docs: 모든 변경은 GitHub issue 부터 생성하도록 CLAUDE.md 명시 (#313, #314)
  - v2.0.x ~ v2.1.x patch 들이 이슈 없이 PR 만으로 진행돼 트래킹 어려워졌던 회고
  - 사소한 hotfix 도 예외 없음. 작은 변경은 묶어 하나의 이슈로 통합 가능
  - 브랜치 명명 강제 (\`feature/{번호}-{설명}\`, \`fix/{번호}-{설명}\`), PR 본문에 \`closes #번호\` 명시

### Breaking
- 기존 workflowData 가 \`{{##reference_method##}}\` / \`{{##upscale_method##}}\` / \`{{##base_style##}}\` 사용 + customField 미정의 시 빈 값 치환. admin 이 customField 추가 또는 workflow JSON 수정 필요

## v2.1.1

### 워크플로우 변수 정리
- chore: 샘플링 placeholder 제거 (#310)
  - \`{{##steps##}}\` / \`{{##cfg##}}\` / \`{{##sampler##}}\` / \`{{##scheduler##}}\` 의 하드코딩된 기본값 제거. admin 이 작업판의 customField 로 직접 정의 (입력 양식 탭)
- chore: \`WELL_KNOWN_FIELD_NAME_TO_ROLE\` snake_case 단일화 — camelCase 항목 (aiModel / imageSize / negativePrompt 등) 제거. \`LEGACY_BASE_FIELD_TO_ROLE\` 는 마이그레이션 전용으로 분리
- chore: width/height placeholder 에 \`image_size 의 \"WxH\" 값에서 자동 추출\` 안내 추가. admin 의 \"사용 가능한 워크플로우 변수\" 섹션이 caption 으로 표시

### UI 디자인
- chore: MetadataFieldInput (베이스 모델 / LoRA 필드) layout 을 다른 customField 와 통일
  - TextField floating label 사용 (별도 Typography 제거)
  - 해제 / 선택 버튼을 endAdornment 로 이동 — 가로폭 축소, 다른 select/number/string 필드와 동일 높이

### Breaking
- 기존 작업판이 workflowData 에 \`{{##steps##}}\` / \`{{##cfg##}}\` / \`{{##sampler##}}\` / \`{{##scheduler##}}\` 사용 + customField 미정의 시 빈 값 치환됨. admin 이 customField 추가 (예: name=\"steps\", type=\"number\", defaultValue=20) 하거나 workflow JSON 에서 직접 값 입력 필요

## v2.1.0

### 신규 기능
- feat: 작업판 admin \"입력 양식\" 탭의 customField 목록을 드래그 앤 드롭으로 재정렬 (#307)
  - accordion 헤더의 드래그 핸들 (⫶⫶) 로 순서 변경. 사용자 페이지에 그대로 반영

### 리팩토링 — 단일 진실 + legacy alias 제거
- refactor: 워크플로우 변수 (placeholder) 정의를 backend / frontend 공유 constants 로 추출 (#308)
  - \`src/constants/workflowVariables.js\` (backend canonical) + \`frontend/src/constants/workflowVariables.js\` (mirror, 수동 동기화)
  - admin 의 \"사용 가능한 워크플로우 변수\" 섹션이 constants 를 loop 으로 표시 — 하드코딩 13개 entry 제거. 카테고리 / 라벨 / 타입 / 기본값 메타데이터로 일관

### Breaking changes
- \`{{##model##}}\` 와 \`{{##upscale##}}\` legacy alias 완전 제거 (#308)
  - v2.0.6 에서 deprecated 처리만 했던 것을 v2.1 에서 제거 — snake_case (\`{{##base_model##}}\` / \`{{##upscale_method##}}\`) 단일화
  - 기존 작업판이 workflowData 에 alias 사용 중이면 admin 이 직접 JSON 수정 필요

## v2.0.6

### 수정 (회귀 — 이미지 업로드 실패)
- fix: \`form-data\` 의존성 누락 — ComfyUI 이미지 업로드 시 \"Cannot find module 'form-data'\" 발생 (#305)
  - \`comfyUIService.uploadImage\` 의 \`require('form-data')\` 가 npm 시절 transitive hoisting 으로 우연히 작동했으나, pnpm 의 strict resolution 에서 누락
  - 이미지 customField (start_frame 등) 업로드 실패 → workflow 의 filename placeholder 가 빈 문자열로 치환 → ComfyUI 가 input 디렉토리 자체를 파일로 열려고 시도해 실패
  - \`package.json\` 에 \`form-data ^4.0.0\` 명시, pnpm-lock 재생성

### 일관성
- chore: \`{{##model##}}\` → \`{{##base_model##}}\` (#305)
  - ComfyUI 템플릿의 customField formatString + workflowData placeholder 모두 snake_case 통일
  - \`{{##model##}}\` 은 backend 의 legacy alias 로 유지 — 기존 작업판 영향 없음

## v2.0.5

### 수정 (회귀 — 데이터 손실)
- fix: Phase B 마이그레이션이 mongoose strict 로 \`baseInputFields\` 를 읽지 못해 데이터 손실 (#303)
  - v2.0.0 release 가 F4 (schema 에서 baseInputFields 제거) 와 Phase B (baseInputFields → customField 이전) 를 동시 포함. 부팅 시 Phase B 가 mongoose 로 워크보드 로드 → schema 가 baseInputFields 를 모르니 strict 가 strip → \"옮길 데이터 없음\" 으로 잘못 판단 → 직후 F4 의 \`\$unset\` 으로 원본 영구 손실
  - \`Workboard.collection.find()\` (raw) 로 schema 우회. update 도 \`collection.updateOne\` 사용. 미래 비슷한 버그 재발 방지

### 복구 도구
- feat: \`scripts/recover-workboard-base-input-fields.js\` — 백업 ZIP 의 \`database/Workboard.json\` 에서 baseInputFields 만 추출해 현재 DB 의 누락된 customField 로 이전 (멱등). v2.0.0 ~ v2.0.4 사이에 데이터 손실 입은 admin 용
  \`\`\`bash
  docker compose run --rm -v /path/to/backup.zip:/tmp/backup.zip backend \\
    node /app/scripts/recover-workboard-base-input-fields.js /tmp/backup.zip
  \`\`\`

## v2.0.4

### 수정
- fix: 베이스 모델 / LoRA 선택 후 경로 대신 이름으로 표시 (#301)
  - MetadataFieldInput 이 value (filename, 경로 포함) 를 그대로 표시해 \`IXL\\nova.safetensors\` 같은 경로 노출. Civitai 모델명 (있으면) 또는 파일 basename 으로 표시 우선. Tooltip / helperText 로 전체 경로 확인 가능. 내부 form value 는 그대로 filename 유지

### 운영
- chore: ComfyUI 응답 본문 로깅 — workflow 제출 실패 시 ComfyUI 가 반환한 validation 메시지 / 에러 본문을 \`❌ ComfyUI 응답 본문\` 로그로 캡쳐. generic \"Failed to submit workflow\" 만으로는 root cause 분석 어렵던 문제

## v2.0.3

### 수정
- fix: ComfyUI workflow injection 의 partial-match 경로 double-escape 회귀 (#299)
  - \`replaceInObject\` 의 부분 일치 분기가 \`escapeForJsonString\` 으로 backslash 를 \`\\\\\` 로 만든 후 JS object 에 저장. 이후 axios 가 JSON.stringify 로 한 번 더 escape — ComfyUI 가 double-escape 된 경로로 수신해 Windows 모델 경로 손상
  - 수정: partial-match 도 full-match 와 동일하게 raw value 사용. JSON.stringify 의 자동 escape 만 적용
  - fallback string-replacement (workflowString 직접 조작) 는 JSON 텍스트 컨텍스트라 escape 유지

## v2.0.2

### 수정
- fix: 작업판 customField 가 비표준 이름 (예: `base_model`) 일 때 \"Missing required fields: workboardId, prompt, aiModel\" 회귀 (#297)
  - v2.0.1 의 \`additionalParams.aiModel\` hoist 는 표준 이름만 매칭. ComfyUI workflow 컨벤션 따라 \`base_model\` 같은 snake_case 이름을 사용하면 추출 실패
  - POST /jobs/generate 가 \`getFieldValueByRole\` 헬퍼 사용 — workboard.additionalInputFields 에서 type=baseModel 필드를 찾아 그 필드 이름으로 inputData 조회. customField name 이 임의여도 작동
- chore: 5개 템플릿의 customField name 을 snake_case 로 통일 — ComfyUI workflow placeholder (\`{{##negative_prompt##}}\` 등) 와 일관된 컨벤션
  - aiModel → base_model
  - imageSize → image_size
  - referenceImageMethod → reference_image_method
  - systemPrompt → system_prompt
  - maxTokens → max_tokens
- chore: \`WELL_KNOWN_FIELD_NAME_TO_ROLE\` 에 snake_case alias 추가 — 사용자 정의 신규 작업판이 snake_case 만으로도 동작

## v2.0.1

### 수정
- fix: 기존 작업판 실행 시 \"Missing required fields: workboardId, prompt, aiModel\" 회귀 (#295)
  - v2.0 (#199 F2) 이후 사용자 페이지는 \`aiModel\` / \`imageSize\` 를 customField dynamic loop 의 \`additionalParams\` 네임스페이스로 전송
  - `POST /api/jobs/generate` 라우트 validation 이 top-level 만 보고 있어 \"Missing required\" 오류 발생
  - \`req.body.aiModel || additionalParams.aiModel\` 패턴으로 hoist. top-level 우선, additionalParams fallback (v1.x 페이로드 backward-compat)
  - imageSize 도 동일 패턴 적용

## v2.0.0

v2.0 메이저 릴리스. 작업판 권한 / 모델·LoRA 노출 정책 / 작업판 풀 커스텀화 / 모델 선택 UI 통합 / supply chain 보안 강화. v1.x 의 hardcoded 입력 정의가 모두 customField 단일 경로로 통일됨.

### 권한 / 노출 정책 (#198)
- feat: `Group` 모델 신설 — `name`, `description`, `permissions[]`, `isDefault` (#270, #271)
  - 신규 가입자가 자동으로 들어가는 \"기본 그룹\" 1개 자동 생성·유지. 멱등 마이그레이션
  - admin 은 implicit all-access — 그룹과 무관하게 모든 작업판 접근 가능
- feat: 작업판 단위 접근 권한 — `Workboard.allowedGroupIds[]` (#272, #273)
  - 사용자 ↔ 작업판 관계는 UNION 규칙: `workboard.allowedGroupIds ∩ user.groupIds ≠ ∅` 이면 접근 허용
  - 백엔드 미들웨어 (`requireWorkboardAccess`) + GET 필터 빌더 (`buildWorkboardAccessFilter`) 신설. 기존 라우트가 자동 필터링
- feat: 작업판의 모델·LoRA 노출 정책 — `modelExposurePolicy` / `loraExposurePolicy` (`full` / `whitelist`) + 화이트리스트 배열 (#274)
  - 사용자 페이지의 model/LoRA picker 가 작업판 컨텍스트에서 backend 의 정책을 받아 자동 필터링
  - `__no_models_in_whitelist__` sentinel 로 빈 화이트리스트 = \"0개 노출\" 정책 명시
- feat: admin 그룹 관리 페이지 (`/admin/groups`) (#275)
  - 그룹 CRUD + 멤버 관리 다이얼로그. admin 은 implicit all-access 라 멤버 목록에서 제외 표시
- feat: 작업판 admin 에 화이트리스트 picker (#276)
  - 직접 텍스트 입력 + \"선택\" 버튼으로 `MetadataPickerModal` (multi-add 모드) 호출. ComfyUI 서버 only

### 작업판 풀 커스텀화 (#199)
- feat: customField 헬퍼 + 스키마 — \`additionalInputFields\` 의 \`role\` 필드 (#278)
  - 13개 role enum (model, prompt, negativePrompt, systemPrompt, imageSize, seed, temperature, maxTokens, lora, referenceImage, referenceImageMethod, stylePreset, upscaleMethod)
  - `getFieldByRole` / `getFieldValueByRole` / `indexFieldsByRole` 헬퍼 — 서비스 코드가 필드 이름 대신 의미로 lookup
- feat: 자동 마이그레이션 (#279, #280)
  - 기존 `baseInputFields` 의 well-known 키 9개 → `additionalInputFields` role-부여 항목으로 자동 변환 (멱등)
  - `WELL_KNOWN_FIELD_NAME_TO_ROLE` 매핑으로 v1.x 데이터 호환 (`prompt` / `negativePrompt` / `seed` 등 inputData 직접 키 fallback)
- refactor: 서비스 코드 role 기반 lookup (#280)
  - `queueService` 의 9개 위치를 `getFieldValueByRole` 헬퍼로 마이그레이션. `inputData.aiModel` 같은 직접 access 제거
  - `inputData.additionalParams[fieldName]` fallback 으로 사용자 페이지 dynamic loop 의 nested 입력값도 인식
- feat: 입력 타입 baseModel / lora 신설 (#281)
  - role UI 대신 입력 타입으로 의미 부여. \"베이스 모델\" / \"LoRA\" 타입 선택 시 자동으로 `type → role` 매핑되어 서비스 코드 인식
  - 사용자가 비표준 이름을 써도 의미 추론 가능
- feat: 사용자 페이지 baseModel/lora picker 렌더 (#282)
  - 신규 컴포넌트 `MetadataFieldInput` — TextField + 선택/해제 + `MetadataPickerModal` (select-single)
  - `workboardId` 전달로 작업판 노출 정책 (#198) 자동 적용
- feat: 템플릿 customField 화 + workflow 무결성 fix (#283)
  - 5개 템플릿 (ComfyUI image/video, OpenAI image/text, OpenAI Compatible text, Gemini image/text) 의 `baseInputFields` 비우고 `additionalInputFields` 에 baseModel/lora/select/string/number 정의
  - ComfyUI 의 aiModel customField 에 `formatString='{{##model##}}'` 명시 — workflow placeholder 매칭
  - 종전 회귀 fix: `getFieldValueByRole` 가 nested `additionalParams` 도 fallback 조회 (ComfyUI workflow placeholder 가 dynamic loop 입력으로도 정상 치환)
- refactor: 사용자 페이지 hardcoded UI 제거 (#284)
  - `ImageGeneration.js` 의 aiModel select / 이미지 크기 select / \"모델 목록\" 버튼 + MetadataPickerModal 제거. baseInputFields 기반 기본값 / {key,value} 매핑 wrap 제거
  - `PromptGeneratorPanel.js` 의 aiModel select / referenceImages / systemPrompt hardcoded UI 제거
  - 전체 코드 -498 / +43 lines
- refactor: 작업판 admin \"기초 입력값\" 탭 제거 (#286)
  - 305 lines 의 baseInputFields 편집 UI 제거. 모든 입력 정의를 \"커스텀 필드\" 탭으로 일원화
  - 탭 재인덱싱: 0=기본 정보 / 1=커스텀 필드 / 2=권한 노출 / 3=ComfyUI 워크플로우
- refactor: baseInputFields 스키마 + role 필드 drop (#287)
  - `Workboard.baseInputFields` subschema 제거 (9개 키)
  - `inputFieldSchema.role` 필드 제거 — type=baseModel/lora 추론으로 대체
  - 마이그레이션 `dropBaseInputFieldsSchema` 가 DB 잔존 데이터 `$unset`

### 모델 선택 UI 통합 (#200, #260)
- feat: 모델 메타데이터 + 동적 조회 — ModelCache 확장 + provider 별 동기화 (#245, #246, #247, #248, #249)
  - Civitai 메타데이터 (description, baseModel, trigger words, 섬네일) 자동 fetch
  - OpenAI/Gemini provider 의 동적 모델 목록 조회 (`/v1/models` 등)
  - `routes/servers.js` 의 detailed endpoint — 4종 server type 통일 응답 구조 (LoRA 패턴과 동일)
- feat: 모델 picker 그리드 + 통합 위계 (#250, #251, #261, #262, #263, #264, #265, #267, #268, #269)
  - `MetadataPickerModal` — kind=lora|model 공통 picker. select-single / multi-add / prompt-insert 3개 모드
  - `MetadataItemCard` — 메타데이터 / 섬네일 / trigger words / 상세 expand 통합 카드
  - `MetadataItemGrid` — 반응형 그리드
  - 인라인 LoRA 프롬프트 insert 모드 — 사용자가 LoRA 카드 클릭으로 프롬프트에 `<lora:...>` 자동 삽입
  - Civitai 섬네일 URL 변환 (\`/original=true/\` → `/width=N/`) 으로 로딩 속도 개선 (#267)
  - 빈 배열 reference 안정화로 무한 fetch loop 회귀 차단 (#266)

### 작업판 모델 타입 제약 (#252)
- feat: `Workboard.allowedModelTypes[]` (#253, #254, #255)
  - civitai.baseModel 기반 작업판 별 base 모델 타입 화이트리스트 (SDXL / Flux / 등)
  - 모델 picker 의 baseModel 필터 옵션이 작업판 제약과 교집합으로 노출
  - 메타데이터 없는 모델 (Civitai 미등록 / hash 없음) 은 노출 — custom merge 등 일상적 케이스 대응

### Phase 6: apiFormat 필드 완전 제거 (#181 마지막)
- refactor: Workboard.apiFormat 필드 + dispatcher fallback 제거 (#244)
  - v1.8.0 의 capability 조합 기반 라우팅으로 완전 전환 완료. ~130 lines cleanup

### 운영 / 안정성
- fix: stuck sync cleanup 마이그레이션 (#256, #257)
- perf: 섬네일 공유 메모리 최적화 (#258, #259)
- fix: model picker 의 빈 배열 reference 무한 fetch loop 회귀 (#266)
- chore: 작업판 admin \"커스텀 필드\" 탭 평탄화 — hardcoded 3개 필드 + 외부 accordion 제거 (#288)
- chore: \"입력 양식\" 으로 라벨 변경 (사용자 피드백: \"필드\" 가 프로그래머 용어) + 오타 수정 (#289)

### Supply Chain 보안 강화
- chore: npm → pnpm 마이그레이션 + Node 22 + `minimumReleaseAge: 1440` (#290)
  - Mini Shai-Hulud (TanStack/SAP/Mistral/UiPath npm worm, 2026-04~05) 대응
  - pnpm v11+ 기본 24시간 release cooldown 으로 신규 published 패키지 자동 차단
  - `strict-dep-builds` 로 침해 dep 의 lifecycle script 자동 봉쇄
  - `--frozen-lockfile` 로 lockfile mutation 차단
  - 3개 package-lock.json → 3개 pnpm-lock.yaml, 3개 Dockerfile 갱신
  - sharp 0.32 → 0.34 (Node 22 호환 + lifecycle script 불필요)

### Breaking changes / 마이그레이션
- `Workboard.baseInputFields` 스키마 제거 — 데이터는 자동 마이그레이션 (`backfillCustomFieldRoles`) 으로 `additionalInputFields` 로 이전됨
- `inputFieldSchema.role` 필드 제거 — type=baseModel/lora 로 의미 추론 (사용자 admin UI 변경 없음)
- v1.x 백업 파일 복원 시 baseInputFields 데이터 손실 (mongoose strict mode). v2.0 으로 운영 마이그레이션 완료 후의 백업은 영향 없음
- npm → pnpm 전환 — 로컬 개발자 워크플로우 변경 필요. `corepack enable && pnpm install` 사용
